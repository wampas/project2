<%= form_for ([@article, @article.comments.build]) do |a| %>
<div class="form-group">
  <%= a.label 'Leave a Comment' %>
  <%= a.text_area :body, class: "form-control" %>
</div>

<%= a.submit 'Submit', class: "btn btn-default" %>
<% end %>
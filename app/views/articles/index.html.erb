<h1>Articles</h1>

<% if policy(@article).update? %>
  <%= link_to "New article", new_article_path(@article) %>
<% end %>

<p>
  <ul>
    <% @articles.each do |a| %>
    <li><%= link_to a.title, article_path(a.id.to_s) %></li>
    <% end %>
  </ul>
</p>
<p>
  
</p>

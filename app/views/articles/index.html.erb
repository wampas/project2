<h1>Articles</h1>

<p>
  <ul>
    <% @articles.each do |article| %>
      <li id="article_id-<%= article.id %>">
        <%= link_to article.title, article %>
        <%= link_to 'Delete', article_path(article), method: :delete, remote: true %>
      </li>
    <% end %>
  </ul>
</p>
<p>
  
</p>
